#ifndef REQUEST_HPP
#define REQUEST_HPP

#include "String.hpp"

class   Request
{
    public :
        Request( void );
        Request(String _request);
        // Request(const Request& copy);

};

#endif