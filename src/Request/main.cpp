#include "Server.hpp"
// #include "Parser.hpp"
// #include "Checker.hpp"



int main()
{

	// Parser parsing(av[1]);
	// Checker	check(parsing.getServers());
	// check.fullCheck();
	// ServerData servers(parsing.getServers());
	
	Server	s;
}